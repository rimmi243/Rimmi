#include <stdio.h>
int main()
{
    int l,a,p;
    printf("enter side of square:");
    scanf("%d", &l);
    a=l*l;
    printf("area of square: %d\n", a);
    p=4*l;
    printf("perimeter of square: %d\n", p);
}
